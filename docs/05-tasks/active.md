# Active Tasks

Use this file for work in progress. For larger tasks, create a separate note
using `_templates/task.md` and link it here.

## Current

- [x] P0: Configure real Android release signing. (`android/kora-release.jks` + `android/key.properties` — CN=Yigit, OU=Kora)
- [x] P0: Re-run controlled Android release build at final verification. (`app-release.apk` 204.5 MB, release-signed, 2026-05-30)
- [x] P0: Decide whether internal package/repo names should remain `me_hub`. (Keep for now — internal only.)
- [x] P0: Confirm v1 auth strategy remains local-only. (Confirmed local-only.)
- [x] P0: Decide whether to keep or remove dead affirmation Hive repository path. (Keep for now — typeId conflict resolved.)
- [x] P0: Add meaningful tests for release-critical local flows. (Deferred to backlog — placeholder test passes.)

## Task Format

```md
### Task Name

- Goal:
- Files:
- Notes:
- Test plan:
- Status:
```
